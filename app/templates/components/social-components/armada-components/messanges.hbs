<h3 class="align-left">
  Membership Requests
</h3>

{{#each me.user.armada.armadaMembershipRequests as |request index|}}
  <div class="list-item flex-container center-y">
    <div class="index">{{math index "+" 1}}</div>
    <div class="name">{{request.user.name}}</div>
  </div>
{{/each}}
