<div class="menu-content">
    {{#each buttons as |button index|}}
        {{#lr-button index=index name=button.name focused=button.focused buttonAction=(action button.action)}}
            {{fa-icon button.icon size="lg"}} {{button.display}}
        {{/lr-button}}
    {{/each}}
</div>
