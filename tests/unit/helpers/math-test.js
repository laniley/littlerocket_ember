import { math } from 'littlerocket/helpers/math';
import { module, test } from 'qunit';

module('Unit | Helper | math');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = math([2, '+', 2]);
  assert.equal(result, 4);
});
