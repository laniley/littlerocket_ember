import { ifEquals } from '../../../helpers/if-equals';
import { module, test } from 'qunit';

module('Unit | Helper | if equals');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = ifEquals(42);
  assert.ok(result);
});
